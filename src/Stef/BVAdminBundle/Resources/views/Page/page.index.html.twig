{% extends 'StefBVAdminBundle::form.html.twig' %}
{% set pagetitle = 'Pagina overzicht' %}

{% block form %}

    <ul>
        {% for page in pages %}
            <li><a href="{{ path('stef_bvadminbundle_edit_page', { 'id': page.id }) }}">{{ page.title|e }}</a></li>
        {% endfor %}
    </ul>

{% endblock %}