{% extends 'StefBVBundle::layout.html.twig' %}

{% block body %}

    <div id="main-wrapper">
        <div id="main" class="container">
            <h1>{{ page.title }}</h1>
            {{ page.body|raw }}

            <div style="width:100%; height: 700px">
                {{ google_map(map) }}
            </div>
        </div>
    </div>
{% endblock %}